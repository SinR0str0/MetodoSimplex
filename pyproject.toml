[project]
name = "optimizacion-simplex"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "scipy>=1.11.0",
    "numpy>=1.24.0",
]

[tool.vercel]
# 🚨 ESTO LE DICE A VERCEL EXACTAMENTE DÓNDE BUSCAR:
entrypoint = "api.solve_mpl:handler"